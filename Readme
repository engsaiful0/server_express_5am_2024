mkdir registration-server
cd registration-server
npm init -y
npm install express

npm install cors
node server.js

npm install mongoose
#HTTPie is an alternative tool that provides a cleaner syntax for testing APIs. First, you'll need to install it if it's not already installed:
pip install httpie //Requreid python

#for image
npm install multer 

npm install  bcryptjs jsonwebtoken  dotenv
#nodemon is a tool that automatically restarts your Node.js application when it detects changes in your project files.
npm install --save-dev nodemon
